apiVersion: v1
kind: Secret
metadata:
  name: microservice-secret
  namespace: default
  labels:
    app: microservice
type: Opaque
data:
  # Placeholder value: base64("changeme") = "Y2hhbmdlbWU="
  # In CI this Secret is recreated from the GitHub repository secret API_KEY,
  # so this committed value is intentionally non-sensitive.
  API_KEY: Y2hhbmdlbWU=
